% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_model.R
\name{add_bf_backend}
\alias{add_bf_backend}
\title{Add BayesFlow Backend to Existing Model}
\usage{
add_bf_backend(
  model,
  bayesflow_model,
  backend_args_bf = list(batch_size = 64L, n_posterior_samples = 1000L),
  set_active = TRUE
)
}
\arguments{
\item{model}{Existing rctbp_model object}

\item{bayesflow_model}{BayesFlow/Keras model (Python object via reticulate).
Load using \code{\link[=load_bf_model]{load_bf_model()}} or Python's keras.models.load_model().}

\item{backend_args_bf}{BayesFlow configuration (batch_size, n_posterior_samples)}

\item{set_active}{Whether to switch to BayesFlow backend (default TRUE)}
}
\value{
The modified model with BayesFlow backend added
}
\description{
Adds a BayesFlow model to an existing rctbp_model that only has brms.
This enables dual-backend support, allowing comparison between backends
or switching to the faster BayesFlow backend.
}
\examples{
\dontrun{
# Start with brms-only model
model <- build_model(predefined_model = "ancova_cont_2arms")
model@active_backend  # "brms"

# Add BayesFlow backend
bf_model <- load_bf_model("ancova_cont_2arms")
model <- add_bf_backend(model, bf_model)
model@active_backend  # "bf" (auto prefers BayesFlow)

# Force brms backend even with BayesFlow available
model@backend <- "brms"
model@active_backend  # "brms"
}
}
\seealso{
\code{\link[=build_model]{build_model()}}, \code{\link[=load_bf_model]{load_bf_model()}}
}
