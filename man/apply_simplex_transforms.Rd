% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization_transforms.R
\name{apply_simplex_transforms}
\alias{apply_simplex_transforms}
\title{Apply Simplex Transforms to Parameters (Structured Return)}
\usage{
apply_simplex_transforms(xs, search_specs)
}
\arguments{
\item{xs}{Named list of optimizer values from bbotk}

\item{search_specs}{Named list of simplex specifications from objectives}
}
\value{
List with:
\itemize{
\item \code{crossed}: Named list ready for build_conditions()
\item \code{ilr_values}: Named list of original ILR coordinates (for archive)
\item \code{simplex_values}: Named list of actual proportions (for archive)
}
}
\description{
Transforms optimizer parameter values (ILR coordinates, bounded proportions)
back to the original simplex-constrained space. Returns a structured list
with separate components for conditions, ILR values, and simplex values.
}
\details{
See also \code{\link[=apply_simplex_transforms_flat]{apply_simplex_transforms_flat()}} in \code{pareto_optimize.R} which
returns a flat list (no nested structure).

For 2-arm/2-look cases: optimizer directly provides bounded proportions,
transform applies the allocation/timing format.

For k-arm/k-look cases: optimizer provides k-1 ILR coordinates in separate
parameters (e.g., p_alloc_ilr_1, p_alloc_ilr_2). This function:
\enumerate{
\item Collects ILR coordinates into a vector
\item Applies inverse ILR to get unconstrained simplex
\item Applies min constraint via constrained_simplex()
\item Formats for build_conditions()
}
}
\keyword{internal}
