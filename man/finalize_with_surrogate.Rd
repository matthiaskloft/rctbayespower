% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization_internal.R,
%   R/optimization_internal_backup.R
\name{finalize_with_surrogate}
\alias{finalize_with_surrogate}
\title{Finalize Optimization Using Surrogate Model}
\usage{
finalize_with_surrogate(
  mbo_config,
  instance,
  objectives,
  fidelity_schedule,
  optimum,
  sims_final_run,
  n_cores,
  bf_args,
  brms_args,
  verbosity
)

finalize_with_surrogate(
  mbo_config,
  instance,
  objectives,
  fidelity_schedule,
  optimum,
  sims_final_run,
  n_cores,
  bf_args,
  brms_args,
  verbosity
)
}
\arguments{
\item{mbo_config}{MBO configuration from setup_mbo_components}

\item{instance}{bbotk optimization instance}

\item{objectives}{rctbp_objectives object}

\item{fidelity_schedule}{Data frame with fidelity schedule (uses max n_sims)}

\item{optimum}{Optimum selection method: "surrogate" or "empirical"}

\item{sims_final_run}{Number of simulations for final confirmation run}

\item{n_cores}{Parallel cores}

\item{bf_args}{BayesFlow arguments}

\item{brms_args}{brms arguments}

\item{verbosity}{Output level}

\item{trim_param_space}{Trimming proportion for parameter space constraint (0-0.5)}
}
\value{
List with result_df and power_analysis

List with result_df and power_analysis
}
\description{
Uses the fitted surrogate model to find the best point (smoothed prediction),
then runs a final confirmation simulation at that point to get precise
metrics with standard errors.

Uses the fitted surrogate model to find the best point (smoothed prediction),
then runs a final confirmation simulation at that point to get precise
metrics with standard errors.
}
\keyword{internal}
