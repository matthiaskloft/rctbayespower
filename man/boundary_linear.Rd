% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundaries.R
\name{boundary_linear}
\alias{boundary_linear}
\title{Linear Boundary}
\usage{
boundary_linear(start = 0.999, end = 0.975)
}
\arguments{
\item{start}{Threshold at first analysis (info_frac approaching 0). Default 0.999.}

\item{end}{Threshold at final analysis (info_frac = 1). Default 0.975.}
}
\value{
A function that takes information fraction and returns threshold.
}
\description{
Creates a boundary function that interpolates linearly from a start value
at the first look to an end value at the final analysis.
}
\details{
Formula: \code{threshold = start + (end - start) * info_frac}

For success boundaries, typically \code{start > end} (stringent early, relaxed late).
For futility boundaries, typically \code{start < end} (lenient early, stringent late).
}
\examples{
# Success boundary: strict early, relaxed late
scs_boundary <- boundary_linear(start = 0.999, end = 0.975)
scs_boundary(0.5)  # 0.987

# Futility boundary: lenient early, strict late
ftl_boundary <- boundary_linear(start = 0.70, end = 0.90)
ftl_boundary(0.5)  # 0.80
}
\seealso{
\code{\link[=boundary_obf]{boundary_obf()}}, \code{\link[=boundary_pocock]{boundary_pocock()}}, \code{\link[=boundary_power]{boundary_power()}}
}
