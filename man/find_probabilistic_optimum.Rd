% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization_internal.R
\name{find_probabilistic_optimum}
\alias{find_probabilistic_optimum}
\title{Find Probabilistic Optimum}
\usage{
find_probabilistic_optimum(
  result,
  alpha = 0.95,
  target = NULL,
  n_candidates = 1000,
  use_log_n = TRUE
)
}
\arguments{
\item{result}{An \code{rctbp_optimization_result} object from \code{\link[=optimization]{optimization()}}}

\item{alpha}{Required confidence level for P(power >= target) (default 0.95).
Higher values (e.g., 0.99, 0.999) are more conservative.}

\item{target}{Target power value. If NULL (default), extracted from
the optimization objectives.}

\item{n_candidates}{Number of candidate n values to evaluate (default 1000)}

\item{use_log_n}{Whether to use log(n) transform for GP input (default TRUE).
Recommended for power functions which have diminishing returns.}
}
\value{
A list with:
\describe{
\item{n_opt}{Optimal sample size (ceiling-rounded to integer)}
\item{p_feas}{Feasibility probability P(power >= target) at n_opt}
\item{predicted_power}{GP-predicted power at n_opt}
\item{ci_lower, ci_upper}{95\% confidence interval for predicted power}
\item{target}{Target power value used}
\item{alpha}{Confidence level used}
\item{profile}{Data frame with P_feas for all candidate n values}
}
}
\description{
Finds the minimum sample size where the GP-predicted probability of
achieving target power exceeds a required confidence level.
}
\details{
This function implements probabilistic constraint-based sample size
determination. Instead of finding n where predicted power = target,
it finds the minimum n where we are alpha-confident that power >= target.

\strong{Method}:
\enumerate{
\item Fits a GP on (log(n), logit(power)) from archive data
\item For each candidate n, computes P_feas = P(power >= target) using
the GP's predictive distribution
\item Returns the minimum n where P_feas >= alpha
}

\strong{Why logit(power)?}
\itemize{
\item Power is bounded in (0, 1); logit transforms to unbounded (-Inf, Inf)
\item GPs work better on approximately Gaussian, unbounded targets
\item Predictions back-transformed to probability scale
}

\strong{Choosing alpha}:
\itemize{
\item alpha = 0.95: 95\% confident power >= target (standard)
\item alpha = 0.99: 99\% confident (more conservative, larger n)
\item alpha = 0.999: Very high confidence for critical studies
}
}
\examples{
\dontrun{
result <- optimization(obj, n_sims = 200, max_evals = 30)

# Find minimum n with 95\% confidence of achieving target
prob_opt <- find_probabilistic_optimum(result, alpha = 0.95)
prob_opt$n_opt
prob_opt$p_feas

# More conservative: 99\% confidence
prob_opt_99 <- find_probabilistic_optimum(result, alpha = 0.99)

# Plot P_feas profile
plot(prob_opt$profile$n, prob_opt$profile$p_feas, type = "l",
     xlab = "Sample Size", ylab = "P(power >= target)")
abline(h = 0.95, lty = 2, col = "red")
abline(v = prob_opt$n_opt, lty = 2, col = "blue")
}
}
\seealso{
\code{\link[=find_surrogate_optimum]{find_surrogate_optimum()}}, \code{\link[=optimization]{optimization()}}
}
