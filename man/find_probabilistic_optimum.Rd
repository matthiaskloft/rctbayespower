% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization_postprocessing.R
\name{find_probabilistic_optimum}
\alias{find_probabilistic_optimum}
\title{Find Probabilistic Optimum}
\usage{
find_probabilistic_optimum(
  result,
  alpha = 0.95,
  target = NULL,
  n_candidates = 1000,
  use_log_n = TRUE
)
}
\arguments{
\item{result}{An \code{rctbp_pareto_result} object from \code{\link[=pareto_optimize]{pareto_optimize()}}}

\item{alpha}{Required confidence level for P(power >= target) (default 0.95).
Higher values (e.g., 0.99, 0.999) are more conservative.}

\item{target}{Target power value. If NULL (default), uses 0.80.}

\item{n_candidates}{Number of candidate n values to evaluate (default 1000)}

\item{use_log_n}{Whether to use log(n) transform for GP input (default TRUE).
Recommended for power functions which have diminishing returns.}
}
\value{
A list with:
\describe{
\item{n_opt}{Optimal sample size (ceiling-rounded to integer)}
\item{p_feas}{Feasibility probability P(power >= target) at n_opt}
\item{predicted_power}{GP-predicted power at n_opt}
\item{ci_lower, ci_upper}{Confidence interval for predicted power (level = alpha)}
\item{target}{Target power value used}
\item{alpha}{Confidence level used}
\item{profile}{Data frame with P_feas for all candidate n values}
}
}
\description{
Finds the minimum sample size where the GP-predicted probability of
achieving target power exceeds a required confidence level.
}
\details{
This function implements probabilistic constraint-based sample size
determination. Instead of finding n where predicted power = target,
it finds the minimum n where we are alpha-confident that power >= target.

\strong{Method}:
\enumerate{
\item Fits a GP on (log(n), logit(power)) from archive data
\item For each candidate n, computes P_feas = P(power >= target) using
the GP's predictive distribution
\item Returns the minimum n where P_feas >= alpha
}
}
\seealso{
\code{\link[=find_surrogate_optimum]{find_surrogate_optimum()}}, \code{\link[=pareto_optimize]{pareto_optimize()}}
}
