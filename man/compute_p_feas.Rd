% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization_transforms.R
\name{compute_p_feas}
\alias{compute_p_feas}
\title{Compute Feasibility Probability}
\usage{
compute_p_feas(mu, sigma, target, eps = 1e-09)
}
\arguments{
\item{mu}{Predictive mean on logit(power) scale}

\item{sigma}{Predictive standard deviation on logit(power) scale}

\item{target}{Target power value (e.g., 0.80)}

\item{eps}{Small value to avoid log(0) (default 1e-9)}
}
\value{
Probability that power >= target
}
\description{
Computes P(power >= target) from GP predictive distribution on logit scale.
}
\details{
Uses the formula:

\if{html}{\out{<div class="sourceCode">}}\preformatted{P_feas = Phi((mu - logit(target)) / sigma)
}\if{html}{\out{</div>}}

where Phi is the standard normal CDF.
}
\keyword{internal}
