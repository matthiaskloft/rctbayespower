% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models_ancova.R
\name{simulate_data_ancova_cont_3arms_batch}
\alias{simulate_data_ancova_cont_3arms_batch}
\title{Simulate ANCOVA Data - Batched Format (3-arm)}
\usage{
simulate_data_ancova_cont_3arms_batch(
  n_sims,
  n_total,
  p_alloc = c(1/3, 1/3, 1/3),
  intercept = 0,
  b_arm_treat = c(0, 0),
  b_covariate = 0,
  sigma = 1
)
}
\arguments{
\item{n_sims}{Number of simulations to generate (batch size)}

\item{n_total}{Sample size per simulation}

\item{p_alloc}{Allocation probabilities (default c(1/3, 1/3, 1/3))}

\item{intercept}{Intercept value (default 0)}

\item{b_arm_treat}{Treatment effect coefficients (length 2 vector)}

\item{b_covariate}{Covariate effect coefficient (default 0)}

\item{sigma}{Residual standard deviation (default 1)}
}
\value{
List with batch-formatted arrays:
\itemize{
\item outcome: matrix (n_sims x n_total)
\item covariate: matrix (n_sims x n_total)
\item group: matrix (n_sims x n_total), values 0/1/2 for arms
\item N: integer, sample size per simulation
\item p_alloc: numeric vector, allocation probabilities
}
}
\description{
Generates multiple 3-arm simulations at once for NPE/BayesFlow efficiency.
Returns matrices instead of data.frames for direct use with neural networks.
}
\details{
Similar to \code{\link[=simulate_data_ancova_cont_2arms_batch]{simulate_data_ancova_cont_2arms_batch()}} but for 3-arm trials.
The group matrix contains values 0, 1, 2 corresponding to control,
treatment 1, and treatment 2 respectively.
}
\examples{
\dontrun{
# Generate 64 simulations with 150 subjects each
batch_data <- simulate_data_ancova_cont_3arms_batch(
  n_sims = 64,
  n_total = 150,
  b_arm_treat = c(0.3, 0.5),
  b_covariate = 0.3
)
dim(batch_data$outcome)  # [64, 150]
}
}
\keyword{internal}
