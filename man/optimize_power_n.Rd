% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pareto_wrappers.R
\name{optimize_power_n}
\alias{optimize_power_n}
\title{Optimize Power vs Sample Size}
\usage{
optimize_power_n(
  design,
  power_metric = "pwr_eff",
  n_range = c(50, 500),
  effect_size,
  constant = list(),
  search = list(),
  n_sims = 1000,
  evals_per_step = 10,
  max_evals = 50,
  n_cores = 1,
  knee_method = "utopia",
  surrogate = "gp",
  bf_args = list(),
  brms_args = list(),
  verbosity = 1
)
}
\arguments{
\item{design}{An \code{\link[=build_design]{build_design()}} object specifying the trial design.}

\item{power_metric}{Power metric to maximize: \code{"pwr_eff"} (default),
\code{"pwr_fut"}, or other power columns from results.}

\item{n_range}{Numeric vector \code{c(lower, upper)} for sample size search bounds.}

\item{effect_size}{Fixed treatment effect size (e
.g., standardized mean difference).
This is the \code{b_arm_treat} parameter for ANCOVA models.}

\item{constant}{Named list of other fixed parameters. Should include all
parameters required by the model except \code{n_total} and \code{b_arm_treat}.}

\item{search}{Optional named list of additional search parameters beyond
\code{n_total} (e.g., threshold parameters).}

\item{n_sims}{Number of simulations per evaluation. Vector for progressive
fidelity (e.g., \code{c(500, 2000)}).}

\item{evals_per_step}{Evaluations per fidelity level.}

\item{max_evals}{Maximum evaluations (when \code{n_sims} is scalar).}

\item{n_cores}{Number of parallel cores.}

\item{knee_method}{Knee selection method: \code{"utopia"}, \code{"min_cost"}, \code{"linear"}.}

\item{surrogate}{Surrogate model: \code{"gp"} or \code{"rf"}.}

\item{bf_args}{BayesFlow-specific arguments.}

\item{brms_args}{brms-specific arguments.}

\item{verbosity}{Output level (0, 1, 2).}
}
\value{
An \code{rctbp_pareto_result} object with \code{optimization_type = "power_n"}.
}
\description{
Finds the Pareto front of power vs sample size for a fixed treatment effect.
This is the most common use case: determining how much power is achievable
at different sample sizes.
}
\examples{
\dontrun{
design <- build_design(predefined_model = "ancova_cont_2arms", target_params = "b_arm2")

result <- optimize_power_n(
  design = design,
  power_metric = "pwr_eff",
  n_range = c(50, 500),
  effect_size = 0.3,
  constant = list(
    thr_dec_eff = 0.975, thr_dec_fut = 0.5,
    thr_fx_eff = 0.2, thr_fx_fut = 0,
    p_alloc = list(c(0.5, 0.5)),
    intercept = 0, b_covariate = 0.3, sigma = 1
  ),
  n_sims = 500,
  max_evals = 30,
  n_cores = 4
)

plot(result)
}
}
\seealso{
\code{\link[=pareto_optimize]{pareto_optimize()}}, \code{\link[=optimize_power_effect]{optimize_power_effect()}}, \code{\link[=optimize_effect_n]{optimize_effect_n()}}
}
