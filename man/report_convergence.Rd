% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_builders.R
\name{report_convergence}
\alias{report_convergence}
\title{Report on Convergence Diagnostics per Condition}
\usage{
report_convergence(x, format = c("cli", "markdown"), heading_level = 2L)
}
\arguments{
\item{x}{An rctbp_power_analysis object with results}

\item{format}{Output format: "cli" for styled console output (default)
or "markdown" for markdown-formatted output suitable for Quarto/RMarkdown.}

\item{heading_level}{Integer specifying the starting heading level for
markdown output (default 2). Use this to integrate reports into documents
where you need headings to start at a different level (e.g., 3 for \verb{###}).}
}
\value{
Invisibly returns the input object. Prints report as side effect.
}
\description{
Displays MCMC convergence diagnostics for each simulation condition,
including Rhat, effective sample sizes, and convergence rates.
}
\details{
The report includes a table with:
\itemize{
\item Condition identifiers and sample sizes
\item Convergence rate: \code{conv_rate} (proportion of converged simulations)
\item R-hat: \code{rhat}, \code{se_rhat}
\item Effective sample sizes: \code{ess_bulk}, \code{se_ess_bulk}, \code{ess_tail}, \code{se_ess_tail}
}

For sequential designs, convergence metrics are from the final analysis look.
BayesFlow results produce NaN for MCMC diagnostics and are silently skipped.
}
\examples{
\dontrun{
# Convergence diagnostics report
report_convergence(result)

# Markdown format for Quarto integration
report_convergence(result, format = "markdown")
}
}
\seealso{
\code{\link[=report]{report()}}, \code{\link[=report_power]{report_power()}}, \code{\link[=report_stopping]{report_stopping()}}
}
