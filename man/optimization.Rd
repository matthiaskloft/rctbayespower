% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization.R
\name{optimization}
\alias{optimization}
\title{Execute Bayesian Optimization}
\usage{
optimization(
  objectives,
  n_sims = 200,
  evals_per_step = 10,
  use_warmup = TRUE,
  warmup_n_sims = NULL,
  warmup_evals = NULL,
  warmup_margin = NULL,
  max_evals = 50,
  n_cores = 1,
  surrogate = c("rf", "gp"),
  acq_function = "auto",
  init_design = c("lhs", "random", "sobol"),
  init_design_size = NULL,
  patience = 30,
  min_delta = 0.001,
  bf_args = list(),
  brms_args = list(),
  refresh = 5,
  run = TRUE,
  verbosity = 1
)
}
\arguments{
\item{objectives}{An rctbp_objectives object from \code{\link[=build_objectives]{build_objectives()}}}

\item{n_sims}{Number of simulations per evaluation. Can be:
\itemize{
\item \strong{Scalar} (default 200): Constant fidelity, all evaluations use same n_sims
\item \strong{Vector} (e.g., \code{c(100, 200, 500, 1000)}): Progressive fidelity where
n_sims increases in discrete steps. Control timing with \code{evals_per_step}.
Early cheap exploration, late precise refinement.
}}

\item{evals_per_step}{Controls evaluations per fidelity level when using
progressive fidelity (vector \code{n_sims}). Ignored when \code{n_sims} is scalar. Can be:
\itemize{
\item \strong{Scalar} (default 10): Same number of evaluations at each level.
Total evals = \code{length(n_sims) * evals_per_step}.
\item \strong{Vector}: Custom evaluations per level. Must have same length as \code{n_sims}.
Total evals = \code{sum(evals_per_step)}.
Example: \code{evals_per_step = c(15, 10, 10, 15)} with \code{n_sims = c(100, 200, 500, 1000)}
means 15 evals at 100, 10 at 200, 10 at 500, 15 at 1000 (50 total).
}}

\item{use_warmup}{Logical (default TRUE). When TRUE and using progressive
fidelity (\code{length(n_sims) > 1}), the first fidelity level is treated as a
warmup phase to estimate reference values for secondary objectives. During
warmup, inverse bonus (\code{min/value}) is used. After warmup, reference-based
bonus (\code{1 - value/reference}) is used. Warmup archive points are recomputed
with the new bonus and seed the main phase GP. Requires secondary objectives
(auto-inferred or explicit via \code{\link[=build_objectives]{build_objectives()}}).}

\item{warmup_n_sims}{Deprecated. Use vector \code{n_sims} instead for multi-fidelity.}

\item{warmup_evals}{Deprecated. Use vector \code{n_sims} with \code{evals_per_step} instead.}

\item{warmup_margin}{Deprecated. No longer used.}

\item{max_evals}{Maximum TOTAL number of objective function evaluations (default 50),
including initial design points. \strong{Ignored when \code{n_sims} is a vector} (total
evals derived from \code{length(n_sims) * evals_per_step}).}

\item{n_cores}{Number of CPU cores for parallel power analysis (default 1).}

\item{surrogate}{Surrogate model type: "rf" (Random Forest, default) or "gp" (Gaussian Process).}

\item{acq_function}{Acquisition function: "auto" (default), "ei" (Expected Improvement),
"cb" (Confidence Bound), or "pi" (Probability of Improvement).}

\item{init_design}{Initial design method: "lhs" (Latin Hypercube, default),
"random", or "sobol". Ignored if warmup provides data.}

\item{init_design_size}{Number of initial evaluations. If NULL, uses 4*d where
d is the number of search parameters. When warmup is enabled, this controls
how many of the best warmup points are selected for re-evaluation with full n_sims.}

\item{patience}{Number of evaluations without improvement before early stopping
(default 30). Only applies to target optimization. Once target is achieved
in the optimal window, ALL subsequent evaluations count toward patience.
The counter resets only when finding smaller parameters that still achieve
target in the optimal window. Set to NULL to disable.}

\item{min_delta}{Defines the optimal window as \verb{[target, target + min_delta)}
(default 0.001). Early stopping is triggered when the target is first
achieved within this window and \code{patience} evaluations pass without finding
smaller parameters that also achieve target in the window.}

\item{bf_args}{Named list of BayesFlow-specific arguments passed to
\code{\link[=power_analysis]{power_analysis()}}. Options: \code{n_posterior_samples}, \code{batch_size}.
Note: Set Python environment before optimization via \code{\link[=bf_status]{bf_status()}}.}

\item{brms_args}{Named list of brms-specific arguments passed to
\code{\link[=power_analysis]{power_analysis()}}. See \code{\link[brms:brm]{brms::brm()}} for available options.}

\item{refresh}{How often to print progress updates (default 5).
Set to 0 to disable progress output, or a positive integer to print
every N evaluations. A CLI progress bar is shown when verbosity >= 1.}

\item{run}{Logical, whether to run immediately (default TRUE).
Set to FALSE to return configuration without executing.}

\item{verbosity}{Output level: 0 (quiet), 1 (normal), 2 (verbose).}
}
\value{
An S7 object of class "rctbp_optimization_result"
}
\description{
Runs Bayesian optimization to find optimal trial designs based on the
objectives specification. Uses mlr3mbo for the optimization backend.
}
\details{
\strong{Optimization Types (Auto-detected):}
\itemize{
\item Single-objective: Uses EGO (Efficient Global Optimization)
\item Target-constrained: Finds minimum search parameter achieving target
\item Multi-objective: Uses ParEGO for Pareto frontier
}

\strong{Surrogate Models:}
\itemize{
\item GP (Gaussian Process): Better for smooth functions, handles noise
\item RF (Random Forest): Better for non-smooth, handles categorical
}
}
\examples{
\dontrun{
design <- build_design(
  model_name = "ancova_cont_2arms",
  target_params = "b_arm2"
)

obj <- build_objectives(
  design = design,
  search = list(n_total = c(50, 500)),
  objectives = list(pwr_eff = target(0.80)),
  constant = list(
    b_arm_treat = 0.3,
    thr_dec_eff = 0.975, thr_dec_fut = 0.5,
    thr_fx_eff = 0.2, thr_fx_fut = 0,
    p_alloc = list(c(0.5, 0.5)),
    intercept = 0, b_covariate = 0.3, sigma = 1
  )
)

result <- optimization(obj, n_sims = 200, max_evals = 30)
print(result)
plot(result)
}
}
\seealso{
\code{\link[=build_objectives]{build_objectives()}}, \code{\link[=target]{target()}}, \code{\link[=power_analysis]{power_analysis()}}
}
