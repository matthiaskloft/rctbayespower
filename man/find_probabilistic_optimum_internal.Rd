% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization_internal.R
\name{find_probabilistic_optimum_internal}
\alias{find_probabilistic_optimum_internal}
\title{Find Probabilistic Optimum from Archive}
\usage{
find_probabilistic_optimum_internal(
  archive_df,
  objectives,
  target = NULL,
  alpha = 0.95,
  n_candidates = 1000,
  use_log_n = TRUE,
  verbosity = 1
)
}
\arguments{
\item{archive_df}{Archive data frame with n_total and power columns}

\item{objectives}{rctbp_objectives object}

\item{target}{Target power value (default: extracted from objectives)}

\item{alpha}{Required confidence level (default 0.95)}

\item{n_candidates}{Number of candidates to evaluate (default 1000)}

\item{use_log_n}{Whether to use log(n) for GP input (default TRUE)}

\item{verbosity}{Verbosity level}
}
\value{
List with:
\itemize{
\item \code{n_opt}: Optimal sample size (ceiling-rounded)
\item \code{p_feas}: Feasibility probability at n_opt
\item \code{predicted_power}: GP-predicted power at n_opt
\item \code{ci_lower}, \code{ci_upper}: Confidence interval for power
\item \code{gp_model}: Fitted GP model (for diagnostics)
}
}
\description{
Finds the minimum sample size where the GP-predicted probability of
achieving target power exceeds the required confidence level alpha.
}
\details{
This function implements the probabilistic constraint approach:
\enumerate{
\item Fits a GP on (log(n), logit(power)) from archive data
\item Evaluates P_feas = P(power >= target) for a grid of n values
\item Finds the minimum n where P_feas >= alpha
}

The GP models logit(power) because:
\itemize{
\item Power is bounded in (0, 1), logit transforms to unbounded
\item GP works better on approximately Gaussian, unbounded targets
}
}
\keyword{internal}
