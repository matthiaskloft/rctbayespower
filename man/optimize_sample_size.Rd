% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_sample_size.R
\name{optimize_sample_size}
\alias{optimize_sample_size}
\title{Optimize Sample Size for Trial Design}
\usage{
optimize_sample_size(
  design,
  objective = c("single", "pareto"),
  n_range = c(50, 500),
  constant = list(),
  n_sims = 1000,
  n_cores = 1,
  target_power = 0.8,
  surrogate = c("gp_power", "gp_score", "rf"),
  score_shape = c("linear", "quadratic", "root"),
  score_scale = c("log", "raw"),
  n_init = 5,
  max_evals = 30,
  patience = 5,
  power_metric = "pwr_eff",
  knee_method = c("utopia", "min_cost", "linear"),
  seed = NULL,
  verbose = TRUE,
  bf_args = list(),
  brms_args = list()
)
}
\arguments{
\item{design}{An \code{\link[=build_design]{build_design()}} object specifying the trial design.}

\item{objective}{Optimization objective: \code{"single"} (find minimum n for
target power) or \code{"pareto"} (find power-vs-n trade-off curve).}

\item{n_range}{Numeric vector \code{c(lower, upper)} for sample size search bounds.}

\item{constant}{Named list of fixed parameters. Must include all parameters
required by the model except \code{n_total}. For example,
\code{list(b_arm_treat = 0.3, thr_dec_eff = 0.975, ...)}.}

\item{n_sims}{Number of simulations per evaluation.}

\item{n_cores}{Number of parallel cores for power analysis.}

\item{target_power}{Target power level (single-objective only, default 0.80).}

\item{surrogate}{Surrogate model strategy (single-objective only):
\itemize{
\item \code{"gp_power"} (default): Custom GP loop fitting logit(power). Theoretically
strongest — exploits smooth monotonic structure.
\item \code{"gp_score"}: Standard bbotk EGO on feasibility score. Simplest pipeline.
\item \code{"rf"}: Random forest on feasibility score. Handles discontinuities natively.
}}

\item{score_shape}{Score decay shape (single-objective only): \code{"linear"} (default),
\code{"quadratic"}, or \code{"root"}.}

\item{score_scale}{Score normalization scale (single-objective only):
\code{"log"} (default, proportional cost) or \code{"raw"} (absolute cost).}

\item{n_init}{Number of initial design points (default 5).}

\item{max_evals}{Maximum evaluations (default 30).}

\item{patience}{Early stopping patience — stop if best score hasn't improved
for this many consecutive evaluations after the initial design (default 5).}

\item{power_metric}{Power metric for Pareto objective (Pareto only, default \code{"pwr_eff"}).}

\item{knee_method}{Knee point selection method (Pareto only):
\code{"utopia"} (default), \code{"min_cost"}, or \code{"linear"}.}

\item{seed}{Random seed for reproducibility.}

\item{verbose}{Whether to show progress messages (default TRUE).}

\item{bf_args}{Named list of BayesFlow-specific arguments.}

\item{brms_args}{Named list of brms-specific arguments.}
}
\value{
\itemize{
\item \code{objective = "single"}: An \code{rctbp_sample_size_result} object with \code{n_optimal},
\code{power_optimal}, \code{feasible} status, \code{archive}, and \code{convergence} trace.
\item \code{objective = "pareto"}: An \code{rctbp_pareto_result} object with \code{pareto_front},
\code{selected_design}, and \code{archive}.
}
}
\description{
Unified entry point for sample size optimization. Finds either the minimum
sample size achieving a target power (single-objective) or the Pareto front
of power vs sample size (multi-objective).
}
\details{
\strong{Single-objective mode} uses Bayesian optimization with a feasibility score
function. The score is 0 when power < target and monotonically decreasing
from n_min to n_max when power >= target, pushing the optimizer toward the
smallest feasible sample size.

\strong{Pareto mode} searches only over \code{n_total}. For multi-dimensional Pareto
search (e.g., n_total + p_alloc), use \code{\link[=pareto_optimize]{pareto_optimize()}} directly.
}
\examples{
\dontrun{
design <- build_design(
  predefined_model = "ancova_cont_2arms",
  target_params = "b_arm2"
)

# Single-objective: find minimum n for 80\% power
result <- optimize_sample_size(
  design,
  objective = "single",
  n_range = c(50, 300),
  constant = list(
    b_arm_treat = 0.3,
    thr_dec_eff = 0.975, thr_dec_fut = 0.5,
    thr_fx_eff = 0.2, thr_fx_fut = 0,
    p_alloc = list(c(0.5, 0.5)),
    intercept = 0, b_covariate = 0.3, sigma = 1
  ),
  target_power = 0.80,
  n_sims = 500,
  max_evals = 20
)
result

# Pareto: power vs sample size trade-off
result_pareto <- optimize_sample_size(
  design,
  objective = "pareto",
  n_range = c(50, 300),
  constant = list(
    b_arm_treat = 0.3,
    thr_dec_eff = 0.975, thr_dec_fut = 0.5,
    thr_fx_eff = 0.2, thr_fx_fut = 0,
    p_alloc = list(c(0.5, 0.5)),
    intercept = 0, b_covariate = 0.3, sigma = 1
  ),
  n_sims = 500,
  max_evals = 30
)
plot(result_pareto)
}
}
\seealso{
\code{\link[=pareto_optimize]{pareto_optimize()}}, \code{\link[=build_design]{build_design()}}, \code{\link[=power_analysis]{power_analysis()}}
}
