% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization.R
\name{search_p_alloc}
\alias{search_p_alloc}
\title{Specify Allocation Probability Search}
\usage{
search_p_alloc(min = 0.1)
}
\arguments{
\item{min}{Minimum proportion acceptable for any group (default 0.1).
Must be in (0, 0.5). For k arms, requires \code{k * min < 1}.}
}
\value{
An object of class "rctbp_search_p_alloc" for use in
\code{\link[=pareto_optimize]{pareto_optimize()}} or wrapper functions' \code{search} argument.
}
\description{
Creates a search specification for optimizing treatment allocation
probabilities. Instead of specifying bounds for each arm, you specify
the minimum acceptable proportion for ANY group.
}
\details{
The allocation probabilities form a simplex (must sum to 1, all non-negative).
This function specifies a constrained region where no group gets less than
\code{min} proportion.

\strong{Internal handling:}
\itemize{
\item 2-arm: Direct sampling of treatment proportion in \verb{[min, 1-min]}
\item k-arm (k>2): ILR transform on (k-1) dimensions, scaled to constraint
}

\strong{Output convention:}
The resulting \code{p_alloc} vector has k entries: \verb{[p_ctrl, p_treat1, p_treat2, ...]}
where the first entry is the control arm probability.
}
\examples{
\dontrun{
# Optimize allocation with at least 20\% per group
result <- pareto_optimize(
  design = design,
  objectives = list(pwr_eff = "maximize", n_total = "minimize"),
  search = list(
    n_total = c(50, 500),
    p_alloc = search_p_alloc(min = 0.2)
  ),
  constant = list(...)
)
}
}
\seealso{
\code{\link[=pareto_optimize]{pareto_optimize()}}, \code{\link[=optimize_sample_size]{optimize_sample_size()}}, \code{\link[=search_looks]{search_looks()}}
}
