% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_model.R
\name{get_model}
\alias{get_model}
\title{Get a Predefined Model}
\usage{
get_model(name, backend = c("brms", "bf"))
}
\arguments{
\item{name}{Character. Name of the predefined model. Use \code{\link[=list_predefined_models]{list_predefined_models()}}
to see available options. Currently supported:
\itemize{
\item "ancova_cont_2arms" - ANCOVA for continuous outcomes, 2 arms
\item "ancova_cont_3arms" - ANCOVA for continuous outcomes, 3 arms
}}

\item{backend}{Character. Backend to use: "brms" (default) or "bf".
\itemize{
\item "brms": Load brms model (default, always available)
\item "bf": Try BayesFlow, fall back to brms with warning if unavailable
}}
}
\value{
An S7 object of class "rctbp_model"
}
\description{
Retrieves a predefined model by name. This is the recommended way to get
models for standard analyses.
}
\details{
By default, models use the brms backend which is always available.
To use BayesFlow, specify \code{backend = "bf"}. If BayesFlow is unavailable,
the function falls back to brms with a warning.

To use a different backend, call \code{get_model()} again with the desired backend.
}
\examples{
# Get model with default brms backend
model <- get_model("ancova_cont_2arms")

# Check which backend is active
model@backend

\dontrun{
# Request BayesFlow backend (falls back to brms if unavailable)
model <- get_model("ancova_cont_2arms", backend = "bf")
}
}
\seealso{
\code{\link[=build_model]{build_model()}} for custom models, \code{\link[=list_predefined_models]{list_predefined_models()}},
\code{\link[=check_bf_available]{check_bf_available()}}
}
