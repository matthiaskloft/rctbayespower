% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models_survival.R
\name{build_model_survival_2arms}
\alias{build_model_survival_2arms}
\title{Create 2-Arm Survival Model}
\usage{
build_model_survival_2arms(
  prior_intercept = NULL,
  prior_treatment = NULL,
  p_alloc = NULL,
  baseline_hazard = NULL,
  hazard_ratio = NULL,
  accrual_rate = NULL,
  followup_time = NULL
)
}
\arguments{
\item{prior_intercept}{Prior for the intercept (log baseline hazard).
If NULL, uses \code{normal(0, 5)}.}

\item{prior_treatment}{Prior for the treatment effect (log hazard ratio).
If NULL, uses \code{normal(0, 2)}.}

\item{p_alloc}{Allocation probability vector (length 2, sums to 1).}

\item{baseline_hazard}{Baseline hazard rate for the control arm.}

\item{hazard_ratio}{Hazard ratio (treatment / control). Values < 1 indicate
treatment benefit.}

\item{accrual_rate}{Patients enrolled per time unit.}

\item{followup_time}{Minimum follow-up time after last patient enrolled.}
}
\value{
An S7 object of class "rctbp_design" for use with \code{\link[=build_conditions]{build_conditions()}}.
}
\description{
Creates a model object for 2-arm survival (time-to-event) power analysis.
Generates data with exponential event times, staggered enrollment, and
administrative censoring.
}
\details{
\strong{Data Generation:}
\enumerate{
\item Enrollment times from \code{\link[=generate_enrollment_times]{generate_enrollment_times()}}
\item Event times: Exponential with rate = \code{baseline_hazard} (control)
or \code{baseline_hazard * hazard_ratio} (treatment)
\item Administrative censoring: \code{max_calendar_time - enrollment_time},
where \code{max_calendar_time = max(enrollment_time) + followup_time}
\item Observed time: \code{min(event_time, admin_censor_time)}
\item Censoring indicator: 0 if event observed, 1 if right-censored (brms convention)
}

\strong{Output columns:}
\itemize{
\item \code{time}: Observed follow-up time
\item \code{censored}: 0 = event observed, 1 = right-censored (brms convention)
\item \code{arm}: Factor with levels "ctrl", "treat_1"
\item \code{enrollment_time}: Calendar time of enrollment
}

\strong{brms formula:} \code{time | cens(censored) ~ arm}
}
\examples{
\dontrun{
model <- build_model_survival_2arms(
  p_alloc = c(0.5, 0.5),
  baseline_hazard = 0.1,
  hazard_ratio = 0.7,
  accrual_rate = 10,
  followup_time = 12
)
}
}
\seealso{
\code{\link[=build_model_ancova_cont]{build_model_ancova_cont()}}, \code{\link[=build_design]{build_design()}}
}
