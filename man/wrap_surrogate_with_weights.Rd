% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization_internal.R
\name{wrap_surrogate_with_weights}
\alias{wrap_surrogate_with_weights}
\title{Wrap Surrogate with MCSE-based Weighted Training}
\usage{
wrap_surrogate_with_weights(surrogate, se_col)
}
\arguments{
\item{surrogate}{mlr3mbo SurrogateLearner object.}

\item{se_col}{Name of the SE column in archive (e.g., "se_pwr_eff").}
}
\value{
Modified surrogate with weighted \code{.update()} method.
}
\description{
Modifies a surrogate model to use inverse-variance weights based on
Monte Carlo Standard Error (MCSE) during training. More precise observations
(lower MCSE) receive higher weight.
}
\details{
Overrides the surrogate's private \code{.update()} method to:
\enumerate{
\item Extract SE values from the archive
\item Compute inverse-variance weights using \code{compute_mcse_weights()}
\item Add weights to the TaskRegr via \code{set_col_roles("obs_weight", "weights_learner")}
}

Both GP (DiceKriging) and RF (ranger) learners support weighted training.
}
\keyword{internal}
