% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundaries.R
\name{boundary_power}
\alias{boundary_power}
\title{Power Family Boundary}
\usage{
boundary_power(base = 0.975, rho = 2)
}
\arguments{
\item{base}{Threshold at final analysis (info_frac = 1). Default 0.975.}

\item{rho}{Shape parameter controlling the boundary curve. Default 2.
\itemize{
\item \code{rho = 2}: Approximates O'Brien-Fleming
\item \code{rho = 1}: Linear interpolation
\item \code{rho = 0.5}: More Pocock-like (less conservative early)
}}
}
\value{
A function that takes information fraction and returns threshold.
}
\description{
Creates a boundary function using a power transformation. Higher values of
\code{rho} create more O'Brien-Fleming-like behavior (conservative early).
}
\details{
Formula: \code{threshold = 1 - (1 - base) * info_frac^(rho/2)}

The \code{rho} parameter controls how quickly the boundary relaxes:
\itemize{
\item Higher rho: More conservative early, faster relaxation later
\item Lower rho: Less conservative early, slower relaxation
}
}
\examples{
# Compare different rho values at info_frac = 0.5
boundary_power(0.975, rho = 3)(0.5)   # More conservative
boundary_power(0.975, rho = 2)(0.5)   # OBF-like
boundary_power(0.975, rho = 1)(0.5)   # Linear
boundary_power(0.975, rho = 0.5)(0.5) # Less conservative
}
\seealso{
\code{\link[=boundary_obf]{boundary_obf()}}, \code{\link[=boundary_pocock]{boundary_pocock()}}, \code{\link[=boundary_linear]{boundary_linear()}}
}
