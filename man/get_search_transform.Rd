% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization_internal.R
\name{get_search_transform}
\alias{get_search_transform}
\title{Get Search Parameter Transform}
\usage{
get_search_transform(type, n_arms = NULL, n_looks = NULL)
}
\arguments{
\item{type}{Transform type: "scalar" (default), "alloc_2arm", "alloc_3arm",
"alloc_karm", "look_1interim", "look_2interim", or a custom function.}

\item{n_arms}{Number of arms (used for allocation transforms)}

\item{n_looks}{Number of looks (used for interim transforms)}
}
\value{
A function that transforms optimizer value(s) to condition format
}
\description{
Returns a transform function for converting optimizer values to the format
expected by \code{\link[=build_conditions]{build_conditions()}}. Handles simplex-constrained parameters
like allocation probabilities and interim look timings.
}
\details{
\strong{Allocation transforms} (\code{p_alloc}):

The optimizer searches over treatment allocation probabilities. The transform
produces the full allocation vector with control probability derived.

Convention:
\itemize{
\item k entries: \verb{[p_ctrl, p_treat1, p_treat2, ...]} - all explicit
\item k-1 entries: \verb{[p_treat1, ...]} - control implicit (1 - sum)
}

For optimization, output always has k entries (control + treatments).\tabular{lll}{
   n_arms \tab Search dims \tab Transform \cr
   2 \tab 1 scalar \tab \code{x -> list(c(1-x, x))} \cr
   3 \tab 2 scalars \tab \code{x -> list(c(1-sum(x), x[1], x[2]))} \cr
   k \tab k-1 scalars \tab Stick-breaking to ensure valid simplex \cr
}


\strong{Interim look transforms} (\code{analysis_at}):

The optimizer searches over interim timing(s). The transform produces
the full \code{analysis_at} vector with final look at 1.0 appended.\tabular{lll}{
   n_looks \tab Search dims \tab Transform \cr
   2 \tab 1 scalar \tab \code{x -> c(x, 1.0)} \cr
   3 \tab 2 scalars \tab Stick-breaking on increments \cr
}
}
\seealso{
\code{\link[=infer_search_type]{infer_search_type()}}, \code{\link[=apply_search_transforms]{apply_search_transforms()}}
}
\keyword{internal}
