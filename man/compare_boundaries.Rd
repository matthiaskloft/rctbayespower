% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_measures.R
\name{compare_boundaries}
\alias{compare_boundaries}
\title{Compare Multiple Boundary Configurations}
\usage{
compare_boundaries(power_result, boundaries)
}
\arguments{
\item{power_result}{An rctbp_power_analysis object with completed results}

\item{boundaries}{Named list of boundary specifications. Each element should
be a list with \code{success} and/or \code{futility} components specifying the
boundary for that configuration. Components can be numeric values or
boundary functions.}
}
\value{
A data frame with one row per boundary configuration per condition,
containing operating characteristics:
\itemize{
\item \code{boundary}: Name of the boundary configuration
\item \code{id_cond}: Condition identifier
\item \code{n_planned}, \code{n_mn}, \code{n_mdn}: Sample size statistics
\item \code{prop_stp_early}, \code{prop_stp_scs}, \code{prop_stp_ftl}: Stopping proportions
}
}
\description{
Evaluate the same simulation results under multiple stopping boundary
specifications. Returns a summary comparing operating characteristics
across all boundary configurations.
}
\examples{
\dontrun{
# Run simulation once
result <- power_analysis(conditions, n_sims = 500, analysis_at = c(0.5, 0.75))

# Compare different boundary configurations
comparison <- compare_boundaries(result, list(
  "Fixed 0.975" = list(success = 0.975, futility = 0.90),
  "OBF-style" = list(success = boundary_obf(0.975), futility = 0.90),
  "Stringent" = list(success = 0.99, futility = 0.95),
  "Linear" = list(
    success = boundary_linear(0.999, 0.975),
    futility = boundary_linear(0.70, 0.90)
  )
))

print(comparison)
}
}
\seealso{
\code{\link[=resummarize_boundaries]{resummarize_boundaries()}}, \code{\link[=boundary_obf]{boundary_obf()}}, \code{\link[=boundary_linear]{boundary_linear()}}
}
