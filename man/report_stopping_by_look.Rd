% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_builders.R
\name{report_stopping_by_look}
\alias{report_stopping_by_look}
\title{Report on Early Stopping per Look and Condition}
\usage{
report_stopping_by_look(x, format = c("cli", "markdown"), heading_level = 2L)
}
\arguments{
\item{x}{An rctbp_power_analysis object with sequential design results}

\item{format}{Output format: "cli" for styled console output (default)
or "markdown" for markdown-formatted output suitable for Quarto/RMarkdown.}

\item{heading_level}{Integer specifying the starting heading level for
markdown output (default 2). Use this to integrate reports into documents
where you need headings to start at a different level (e.g., 3 for \verb{###}).}
}
\value{
Invisibly returns the input object. Prints report as side effect.
}
\description{
Displays detailed early stopping statistics broken down by analysis look
and condition. This report is only available for sequential designs.
}
\details{
The report includes a table with per-look × per-condition statistics:
\itemize{
\item Look identifiers: \code{id_cond}, \code{id_look}, \code{n_analyzed}
\item Power at this look: \code{pwr_scs}, \code{pwr_ftl}
\item Stopping at this look: \code{prop_stp_look}, \code{prop_scs_look}, \code{prop_ftl_look}
\item Cumulative stopping: \code{cumul_stp}
}
}
\examples{
\dontrun{
# Per-look stopping breakdown
report_stopping_by_look(result)

# Markdown format for Quarto integration
report_stopping_by_look(result, format = "markdown")
}
}
\seealso{
\code{\link[=report]{report()}}, \code{\link[=report_power]{report_power()}}, \code{\link[=report_stopping]{report_stopping()}}
}
