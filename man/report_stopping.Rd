% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_builders.R
\name{report_stopping}
\alias{report_stopping}
\alias{report_early_stopping}
\title{Report on Early Stopping Metrics per Condition}
\usage{
report_stopping(x, format = c("cli", "markdown"), heading_level = 2L)

report_early_stopping(x, format = c("cli", "markdown"), heading_level = 2L)
}
\arguments{
\item{x}{An rctbp_power_analysis object with sequential design results}

\item{format}{Output format: "cli" for styled console output (default)
or "markdown" for markdown-formatted output suitable for Quarto/RMarkdown.}

\item{heading_level}{Integer specifying the starting heading level for
markdown output (default 2). Use this to integrate reports into documents
where you need headings to start at a different level (e.g., 3 for \verb{###}).}
}
\value{
Invisibly returns the input object. Prints report as side effect.
}
\description{
Displays early stopping statistics aggregated per condition. This report
is only available for sequential designs with interim analyses.
}
\details{
The report includes a table with per-condition statistics:
\itemize{
\item Sample sizes: \code{n_total}, \code{n_planned}, \code{n_mn}, \code{n_mdn}, \code{n_mode}
\item Stopping proportions: \code{prop_stp_early}, \code{prop_stp_eff}, \code{prop_stp_fut}
\item Modal stopping: \code{prop_at_mode} (proportion stopped at modal N)
\item No decision rate: \code{prop_no_dec}
}
}
\examples{
\dontrun{
# Early stopping summary per condition
report_stopping(result)

# Markdown format for Quarto integration
report_stopping(result, format = "markdown")
}
}
\seealso{
\code{\link[=report]{report()}}, \code{\link[=report_power]{report_power()}}, \code{\link[=report_stopping_by_look]{report_stopping_by_look()}}
}
