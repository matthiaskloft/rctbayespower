% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend_bf.R
\name{get_bf_env_info}
\alias{get_bf_env_info}
\title{Get BayesFlow Environment Information}
\usage{
get_bf_env_info(envname = NULL)
}
\arguments{
\item{envname}{Optional name of Python virtual environment to check.
If NULL (default), uses the currently active environment.}
}
\value{
A list with:
\itemize{
\item \code{device}: "GPU" or "CPU"
\item \code{device_name}: GPU name (e.g., "NVIDIA RTX 4090") or "CPU"
\item \code{cuda_version}: CUDA version string or NULL for CPU
\item \code{cpu_name}: CPU processor name or NULL if unavailable
\item \code{envname}: Name of the Python virtual environment or NULL
\item \code{python_path}: Path to Python executable
\item \code{python_version}: Python version string (e.g., "3.12.0")
\item \code{pkg_versions}: Named list of package versions (bayesflow, keras, torch, numpy)
}
Returns NULL if BayesFlow is not available.
}
\description{
Returns information about the current Python environment and compute device
(CPU or GPU) for BayesFlow inference. Used to display status messages during
power analysis.
}
\examples{
\dontrun{
info <- get_bf_env_info()
if (!is.null(info)) {
  cat("Device:", info$device_name, "\n")
  cat("Environment:", info$envname, "\n")
  cat("Python:", info$python_version, "\n")
  cat("BayesFlow:", info$pkg_versions$bayesflow, "\n")
}

# Check specific environment
info <- get_bf_env_info(envname = "r-rctbayespower")
}
}
