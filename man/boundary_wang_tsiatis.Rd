% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundaries.R
\name{boundary_wang_tsiatis}
\alias{boundary_wang_tsiatis}
\title{Wang-Tsiatis Boundary}
\usage{
boundary_wang_tsiatis(delta = 0.25, alpha = NULL, threshold = NULL)
}
\arguments{
\item{delta}{Shape parameter controlling boundary aggressiveness.
Must be between 0 and 0.5.
\itemize{
\item \code{delta = 0}: O'Brien-Fleming-like (very conservative early)
\item \code{delta = 0.25}: Midpoint between OBF and Pocock
\item \code{delta = 0.5}: Pocock-like (constant across looks)
}}

\item{alpha}{One-sided significance level for frequentist error control.
Must be between 0 and 0.5. Requires the \code{gsDesign} package at call time.
Mutually exclusive with \code{threshold}.}

\item{threshold}{Probability threshold for Bayesian decision rules.
Must be between 0 and 1 (exclusive). Mutually exclusive with \code{alpha}.}
}
\value{
A function that takes a vector of information fractions and returns
a vector of probability thresholds (one per look).
}
\description{
Creates a boundary function from the Wang-Tsiatis one-parameter family,
which smoothly interpolates between O'Brien-Fleming (\code{delta = 0}) and
Pocock (\code{delta = 0.5}) boundaries. Specify either \code{alpha} for frequentist
Type I error control, or \code{threshold} for Bayesian decision rules.
}
\details{
The Wang-Tsiatis boundary family defines z-scale boundaries as:
\deqn{z_k = C \cdot t_k^{\Delta - 0.5}}
where \eqn{t_k} is the information fraction at look \eqn{k}, \eqn{\Delta}
is the shape parameter (\code{delta}), and \eqn{C} is a constant calibrated to
control the overall Type I error.

\strong{Frequentist mode} (\code{alpha}): Requires the \code{gsDesign} package.
At \code{delta = 0}, uses the Lan-DeMets O'Brien-Fleming spending function
(\code{sfLDOF}) for exact results. At \code{delta = 0.5}, uses the Lan-DeMets Pocock
spending function (\code{sfLDPocock}). For intermediate values, calibrates the
constant \eqn{C} via numerical root-finding using \code{gsDesign::gsProbability()}
for multivariate normal integration.

\strong{Bayesian mode} (\code{threshold}): Uses the closed-form probability-scale
formula:
\deqn{\text{threshold}_k = \Phi(\Phi^{-1}(\text{threshold}) \cdot t_k^{\Delta - 0.5})}
No external packages required. At \code{delta = 0.5}, this reduces to a constant
threshold (identical to \link{boundary_pocock(threshold = ...)}). At \code{delta = 0},
produces an OBF-like shape that is qualitatively similar to
\link{boundary_obf(threshold = ...)} but not numerically identical (different
parameterizations).

\strong{Comparison with \code{boundary_power()}}: \code{boundary_power()} is a simple
probability-scale shape function (\code{1 - (1-base) * t^(rho/2)}) without
frequentist alpha control. \code{boundary_wang_tsiatis()} is the principled
z-scale parameterization from the literature with both frequentist and
Bayesian modes.

Unlike \code{\link[=boundary_obf]{boundary_obf()}} and \code{\link[=boundary_pocock]{boundary_pocock()}}, the frequentist mode has
no fallback when \code{gsDesign} is not installed — the general Wang-Tsiatis
calibration requires multivariate normal integration.

Note: \code{threshold} accepts values in (0, 1) exclusive, which differs from
\code{\link[=boundary_obf]{boundary_obf()}} (which requires threshold > 0.5). Values near 0 or 1
produce degenerate boundaries.
}
\examples{
# Bayesian: Wang-Tsiatis shape ending at 0.95 threshold
wt <- boundary_wang_tsiatis(delta = 0.25, threshold = 0.95)
wt(c(0.5, 1.0))

# Pocock-like (delta = 0.5): constant threshold
wt_pocock <- boundary_wang_tsiatis(delta = 0.5, threshold = 0.95)
wt_pocock(c(0.25, 0.5, 0.75, 1.0))  # All 0.95

\dontrun{
# Frequentist: control Type I error at 2.5\% (requires gsDesign)
wt_freq <- boundary_wang_tsiatis(delta = 0.25, alpha = 0.025)
wt_freq(c(0.5, 1.0))
}
}
\references{
Wang, S. K. and Tsiatis, A. A. (1987). Approximately optimal one-parameter
boundaries for group sequential trials. Biometrics 43: 193-199.

Lan, K. K. G. and DeMets, D. L. (1983). Discrete sequential boundaries for
clinical trials. Biometrika 70: 659-663.

Jennison, C. and Turnbull, B. W. (2000). Group Sequential Methods with
Applications to Clinical Trials. Chapman & Hall/CRC.
}
\seealso{
\code{\link[=boundary_obf]{boundary_obf()}}, \code{\link[=boundary_pocock]{boundary_pocock()}}, \code{\link[=boundary_power]{boundary_power()}}
}
