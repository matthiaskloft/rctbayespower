% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_code.R, R/s3_wrappers.R
\name{get_code}
\alias{get_code}
\alias{get_code.rctbp_design}
\alias{get_code.rctbp_conditions}
\alias{get_code.rctbp_power_analysis}
\title{Get Reproducible Code for a Pipeline Object}
\usage{
get_code(x, ...)

\method{get_code}{rctbp_design}(x, ...)

\method{get_code}{rctbp_conditions}(x, ...)

\method{get_code}{rctbp_power_analysis}(x, ...)
}
\arguments{
\item{x}{A pipeline object: \code{rctbp_design}, \code{rctbp_conditions}, or
\code{rctbp_power_analysis}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly returns a character string containing the reconstructed
R code. The code is also printed to the console.
}
\description{
Reconstructs the R code that produced a pipeline object. Walks the
chain from the current object back through its parents
(\code{power_analysis} -> \code{conditions} -> \code{design}) and returns the full
call chain as a formatted, copy-pasteable R script.
}
\details{
Each pipeline constructor (\code{build_design()}, \code{build_conditions()},
\code{power_analysis()}) stores the original call via \code{match.call()}.
\code{get_code()} retrieves these stored calls and formats them as a
self-contained script.

Only arguments explicitly supplied by the user appear in the output --
default values are omitted for clarity. The variable names in the
output follow the convention: \code{design}, \code{conditions}, \code{result}.
}
\examples{
\dontrun{
design <- build_design(
  predefined_model = "ancova_cont_2arms",
  target_params = "b_arm2"
)
conditions <- build_conditions(
  design = design,
  crossed = list(n_total = c(100, 200), b_arm_treat = c(0.3, 0.5)),
  constant = list(
    p_alloc = list(c(0.5, 0.5)),
    intercept = 0, b_covariate = 0.3, sigma = 1,
    thr_dec_eff = 0.975, thr_dec_fut = 0.5,
    thr_fx_eff = 0.2, thr_fx_fut = 0
  )
)
result <- power_analysis(conditions, n_sims = 100, n_cores = 4)

# Print the full reproducible script
get_code(result)

# Works at any level of the pipeline
get_code(conditions)
get_code(design)
}
}
