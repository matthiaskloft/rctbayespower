% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interim_functions.R
\name{interim_continue}
\alias{interim_continue}
\title{Default Interim Function: Always Continue}
\usage{
interim_continue()
}
\value{
A function with the standard interim function interface that
always returns \code{list(decision = "continue", modified_params = NULL)}
}
\description{
Creates an interim decision function that always returns "continue".
This is the default used when \code{analysis_at} is specified but no
\code{interim_function} is provided. Useful for sequential monitoring
without early stopping rules.
}
\details{
This function is used internally as the default when users specify
\code{analysis_at} without an \code{interim_function}. It enables sequential
analysis (multiple looks at the data) without any stopping rules.

The returned function accepts the standard interim function parameters:
\itemize{
\item \code{interim_summaries}: Data frame from \code{compute_measures()}
\item \code{current_n}: Current sample size at this analysis
\item \code{analysis_at}: The scheduled analysis timepoint
\item \code{n_total}: Maximum planned sample size
}
}
\examples{
# Create a design with sequential monitoring but no early stopping
\dontrun{
design <- build_design(
  model = my_model,
  target_params = "b_armtreat_1",
  p_sig_scs = 0.975,
  p_sig_ftl = 0.5,
  analysis_at = c(50, 100, 150),
  interim_function = interim_continue()  # Or omit - this is the default
)
}
}
\seealso{
\code{\link[=interim_futility_only]{interim_futility_only()}}, \code{\link[=interim_success_futility]{interim_success_futility()}}
}
