% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_conditions.R
\name{link}
\alias{link}
\title{Link Parameters for Co-variation}
\usage{
link(...)
}
\arguments{
\item{...}{Named parameters that should co-vary. All must have the same
number of levels.}
}
\value{
An object of class "rctbp_link" containing the linked parameters.
}
\description{
Marks parameters that should vary together (1-to-1) rather than forming
a Cartesian product. Use within the \code{crossed} argument of \code{\link[=build_conditions]{build_conditions()}}.
}
\details{
When used inside \code{crossed}, linked parameters vary together in lockstep
rather than creating all combinations. For example:
\itemize{
\item \code{link(n = c(100, 200), schedule = list(c(50,100), c(100,200)))}
creates 2 conditions where n and schedule vary together
\item Regular params in \code{crossed} still form Cartesian products with
linked groups
}
}
\examples{
\dontrun{
# Simple example: analysis_at varies with n_total
build_conditions(
  design = my_design,
  crossed = list(
    link(
      n_total = c(80, 160),
      analysis_at = list(c(40, 80), c(80, 160))
    ),
    b_arm_treat = c(0, 0.3)
  ),
  constant = list(p_sig_scs = 0.975, p_sig_ftl = 0.5)
)
# Creates 4 conditions: 2 (n_total × analysis_at linked) × 2 b_arm_treat
}

}
