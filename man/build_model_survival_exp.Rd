% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models_survival.R
\name{build_model_survival_exp}
\alias{build_model_survival_exp}
\title{Create Exponential Survival Model}
\usage{
build_model_survival_exp(
  prior_intercept = NULL,
  prior_treatment = NULL,
  n_arms = 2,
  p_alloc = c(0.5, 0.5)
)
}
\arguments{
\item{prior_intercept}{Prior for the intercept (log baseline scale).
If NULL, uses \code{normal(0, 5)}.}

\item{prior_treatment}{Prior for the treatment effect (log hazard ratio).
If NULL, uses \code{normal(0, 2)}.}

\item{n_arms}{Number of arms in the trial (currently must be 2).}

\item{p_alloc}{Allocation probability vector (length 2, sums to 1).}
}
\value{
An S7 object of class \code{rctbp_model}
}
\description{
Creates a model object for exponential survival (time-to-event) power
analysis. Compiles a brms weibull model and returns an \code{rctbp_model} object
for use with the predefined model registry.
}
\details{
The model uses a Weibull family in brms. With shape = 1, the Weibull
reduces to the exponential distribution. brms estimates the shape parameter
from data; it is filtered out of \code{par_names_inference} by \code{\link[=build_design]{build_design()}}.

\strong{brms formula:} \code{time | cens(censored) ~ arm}

This function returns an \code{rctbp_model} object (not \code{rctbp_design}). The
predefined model registry extracts \verb{@inference_model} from it and pairs it
with a sim_fn from \code{\link[=create_survival_exp_sim_fn]{create_survival_exp_sim_fn()}}.
}
\examples{
\dontrun{
model <- build_model_survival_exp(n_arms = 2, p_alloc = c(0.5, 0.5))
}
}
\seealso{
\code{\link[=build_model_survival_exp_2arms]{build_model_survival_exp_2arms()}}, \code{\link[=build_design]{build_design()}},
\code{\link[=show_predefined_models]{show_predefined_models()}}
}
