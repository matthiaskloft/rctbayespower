% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models_ancova.R
\name{simulate_data_ancova_prop_2arms_batch}
\alias{simulate_data_ancova_prop_2arms_batch}
\title{Simulate Proportional ANCOVA Data - Batched Format (2-arm)}
\usage{
simulate_data_ancova_prop_2arms_batch(
  n_sims,
  n_total,
  p_alloc = 0.5,
  intercept = 0,
  b_arm_treat = 0,
  b_covariate = 0,
  sigma = 1
)
}
\arguments{
\item{n_sims}{Number of simulations to generate (batch size)}

\item{n_total}{Sample size per simulation}

\item{p_alloc}{Allocation probability for treatment (default 0.5)}

\item{intercept}{Intercept value on the log-odds scale (default 0)}

\item{b_arm_treat}{Treatment effect coefficient on the log-odds scale}

\item{b_covariate}{Covariate effect coefficient (default 0)}

\item{sigma}{Residual standard deviation on the logit scale (default 1)}
}
\value{
List with batch-formatted arrays:
\itemize{
\item outcome: matrix (n_sims x n_total), values on logit scale
\item covariate: matrix (n_sims x n_total)
\item group: matrix (n_sims x n_total), binary treatment indicator
\item N: integer, sample size per simulation
\item p_alloc: numeric, allocation probability
}
}
\description{
Generates multiple proportional ANCOVA simulations at once for NPE/BayesFlow
efficiency. Returns matrices instead of data.frames for direct use with
neural networks. Outcomes are on the logit scale.
}
\details{
Vectorized batch generation for proportional outcomes. Uses logit-normal model:
\code{outcome = intercept + covariate * b_covariate + group * b_arm_treat + rnorm(0, sigma)}.
Outcome is on the logit scale (unbounded real line).
}
\keyword{internal}
