% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization_search.R
\name{search_looks}
\alias{search_looks}
\alias{print.rctbp_search_looks}
\title{Specify Interim Look Timing Search}
\usage{
search_looks(n = 2, min_spacing = 0.2)

\method{print}{rctbp_search_looks}(x, ...)
}
\arguments{
\item{n}{Number of looks including final analysis (default 2).
Must be >= 2 (at least one interim + final).}

\item{min_spacing}{Minimum proportion of total sample size between
consecutive looks (default 0.2). Requires \code{n * min_spacing <= 1}.}
}
\value{
An object of class "rctbp_search_looks" for use in
\code{\link[=pareto_optimize]{pareto_optimize()}} or wrapper functions' \code{search} argument.
}
\description{
Creates a search specification for optimizing the timing of interim
analyses. Instead of specifying bounds for each look, you specify the
number of looks and minimum spacing between consecutive looks.
}
\details{
The look timings must be ordered (0 < t1 < t2 < ... < 1) and the gaps
between consecutive looks (increments) form a simplex.

\strong{Internal handling:}
\itemize{
\item n=2: Direct sampling of interim proportion in \verb{[min_spacing, 1-min_spacing]}
\item n>2: ILR transform on increments, scaled to satisfy spacing constraint
}

\strong{Output convention:}
The resulting \code{analysis_at} vector has n-1 proportions. The final look at 1.0
is automatically appended by \code{\link[=build_conditions]{build_conditions()}}.
}
\examples{
\dontrun{
# Optimize timing of 3 looks with at least 20\% spacing
result <- pareto_optimize(
  design = design,
  objectives = list(pwr_eff = "maximize", n_total = "minimize"),
  search = list(
    n_total = c(100, 400),
    analysis_at = search_looks(n = 3, min_spacing = 0.2)
  ),
  constant = list(...)
)
}
}
\seealso{
\code{\link[=pareto_optimize]{pareto_optimize()}}, \code{\link[=search_p_alloc]{search_p_alloc()}}
}
