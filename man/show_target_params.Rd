% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/required_fn_args.R
\name{show_target_params}
\alias{show_target_params}
\title{Show Available Target Parameters}
\usage{
show_target_params(x = NULL)
}
\arguments{
\item{x}{One of: an rctbp_design object, a predefined model name (character),
a brmsfit object, or a BayesFlow model. If NULL, shows usage info.}
}
\value{
A character vector of available parameter names (returned invisibly).
If x is NULL, returns NULL invisibly after showing usage info.
}
\description{
Displays the parameter names available for use as \code{target_params} in \code{\link[=build_design]{build_design()}}.
These are the model parameters that can be tested for success/futility.
}
\examples{
\dontrun{
# From a predefined model name
show_target_params("ancova_cont_2arms")

# From a design object
design <- build_design(model_name = "ancova_cont_2arms", target_params = "b_arm2")
show_target_params(design)

# From a brmsfit object
show_target_params(my_brmsfit)
}
}
