% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pareto_optimize.R
\name{pareto_optimize}
\alias{pareto_optimize}
\title{Pareto Optimization for Trial Designs}
\usage{
pareto_optimize(
  design,
  objectives,
  search,
  constant = list(),
  constraint = NULL,
  n_sims = 1000,
  evals_per_step = 10,
  max_evals = 50,
  n_cores = 1,
  knee_method = "utopia",
  surrogate = "gp",
  init_design = "lhs",
  init_design_size = NULL,
  bf_args = list(),
  brms_args = list(),
  verbosity = 1
)
}
\arguments{
\item{design}{An \link{rctbp_design} object specifying the trial design.}

\item{objectives}{Named list of exactly 2 objectives. Each element should be
\code{"maximize"} or \code{"minimize"}. Names must be either power metrics
(e.g., \code{pwr_eff}, \code{pwr_fut}) or search parameters.
Example: \code{list(pwr_eff = "maximize", n_total = "minimize")}.}

\item{search}{Named list of parameter bounds to search over. Each element is a
numeric vector \code{c(lower, upper)}. Can include:
\itemize{
\item Standard parameters: \code{n_total}, \code{b_arm_treat}, threshold parameters
\item Simplex specs: \code{p_alloc = search_p_alloc(min = 0.2)},
\code{analysis_at = search_looks(n = 3)}
}}

\item{constant}{Named list of fixed parameters for all evaluations.}

\item{constraint}{Optional constraint specification. A list with:
\itemize{
\item \code{metric}: Name of metric to constrain (e.g., \code{"pwr_eff"})
\item \code{threshold}: Minimum value required (e.g., \code{0.80} for 80\% power)
Designs not meeting the constraint are excluded from the Pareto front.
}}

\item{n_sims}{Number of simulations per evaluation. Can be a vector for
progressive fidelity (e.g., \code{c(500, 2000)}).}

\item{evals_per_step}{Evaluations per fidelity level when \code{n_sims} is a vector.
Scalar for uniform steps, or vector matching \code{n_sims} length.}

\item{max_evals}{Maximum evaluations (used when \code{n_sims} is scalar).}

\item{n_cores}{Number of parallel cores for power analysis.}

\item{knee_method}{Method for selecting from Pareto front:
\itemize{
\item \code{"utopia"} (default): Closest to ideal point in normalized space
\item \code{"min_cost"}: Minimize second objective
\item \code{"linear"}: Maximum perpendicular distance from endpoints line
}}

\item{surrogate}{Surrogate model type: \code{"gp"} (Gaussian Process, default) or
\code{"rf"} (Random Forest).}

\item{init_design}{Initial design method: \code{"lhs"} (Latin Hypercube, default),
\code{"sobol"}, or \code{"random"}.}

\item{init_design_size}{Number of initial design points (default: 4 * n_params).}

\item{bf_args}{Named list of BayesFlow-specific arguments.}

\item{brms_args}{Named list of brms-specific arguments.}

\item{verbosity}{Output level: 0 (silent), 1 (progress), 2 (debug).}
}
\value{
An \link{rctbp_pareto_result} object containing:
\itemize{
\item \code{pareto_front}: Data frame of Pareto-optimal solutions
\item \code{selected_design}: Knee point selected by \code{knee_method}
\item \code{archive}: All evaluated designs
\item \code{convergence}: Optimization progress trace
}
}
\description{
Core function for multi-objective Bayesian optimization of trial designs.
Finds the Pareto front trading off two objectives (e.g., power vs sample size).
}
\examples{
\dontrun{
design <- build_design(predefined_model = "ancova_cont_2arms", target_params = "b_arm2")

result <- pareto_optimize(
  design = design,
  objectives = list(pwr_eff = "maximize", n_total = "minimize"),
  search = list(n_total = c(50, 500)),
  constant = list(
    b_arm_treat = 0.3,
    thr_dec_eff = 0.975, thr_dec_fut = 0.5,
    thr_fx_eff = 0.2, thr_fx_fut = 0,
    p_alloc = list(c(0.5, 0.5)),
    intercept = 0, b_covariate = 0.3, sigma = 1
  ),
  n_sims = 500,
  max_evals = 30,
  n_cores = 4
)

plot(result)
result@pareto_front
}
}
\seealso{
\code{\link[=optimize_power_n]{optimize_power_n()}}, \code{\link[=optimize_power_effect]{optimize_power_effect()}}, \code{\link[=optimize_effect_n]{optimize_effect_n()}},
\link{plot.rctbp_pareto_result}
}
