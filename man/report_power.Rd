% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_builders.R
\name{report_power}
\alias{report_power}
\alias{report_conditions}
\title{Report on Power Metrics per Condition}
\usage{
report_power(x, format = c("cli", "markdown"), heading_level = 2L)

report_conditions(x, format = c("cli", "markdown"), heading_level = 2L)
}
\arguments{
\item{x}{An rctbp_power_analysis object with results}

\item{format}{Output format: "cli" for styled console output (default)
or "markdown" for markdown-formatted output suitable for Quarto/RMarkdown.}

\item{heading_level}{Integer specifying the starting heading level for
markdown output (default 2). Use this to integrate reports into documents
where you need headings to start at a different level (e.g., 3 for \verb{###}).}
}
\value{
Invisibly returns the input object. Prints report as side effect.
}
\description{
Displays power analysis metrics for each simulation condition, including
success/futility rates, posterior estimates, and convergence diagnostics.
}
\details{
The report includes a table with:
\itemize{
\item Condition identifiers and sample sizes
\item Power metrics: \code{pwr_eff} (efficacy rate), \code{pwr_fut} (futility rate)
\item Posterior estimates: \code{post_mn}, \code{post_sd}
\item Convergence: \code{rhat}, \code{ess_bulk}
}

For sequential designs, power metrics are taken from the final analysis look.
}
\examples{
\dontrun{
# Power metrics report
report_power(result)

# Markdown format for Quarto integration
report_power(result, format = "markdown")
}
}
\seealso{
\code{\link[=report]{report()}}, \code{\link[=report_stopping]{report_stopping()}}, \code{\link[=report_stopping_by_look]{report_stopping_by_look()}}
}
