% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dropout.R
\name{dropout}
\alias{dropout}
\title{Create Dropout Specification}
\usage{
dropout(rate, type = "proportion")
}
\arguments{
\item{rate}{Numeric scalar or vector. For \code{type = "proportion"}: expected
dropout proportion over \code{followup_time} (strictly between 0 and 1).
For \code{type = "hazard"}: constant hazard rate (must be positive).}

\item{type}{Character. \code{"proportion"} (default) or \code{"hazard"}. Recycled
to match \code{length(rate)}.}
}
\value{
For scalar \code{rate}: a single \code{rctbp_dropout} object (a function
\code{f(followup_time) -> hazard_rate} with class \code{c("rctbp_dropout", "function")}).
For vector \code{rate}: a list of \code{rctbp_dropout} objects (ready for grid crossing
via \code{c()} or directly in \code{crossed}).
}
\description{
Specifies expected patient dropout (loss-to-follow-up) for power analysis.
Returns a dropout specification object that converts to a hazard rate
given the follow-up time. Use in the \code{constant} or \code{crossed} arguments
of \code{\link[=build_conditions]{build_conditions()}}.
}
\details{
The dropout model uses an exponential (constant hazard) process:
\itemize{
\item After enrollment, each patient gets \code{dropout_time ~ Exp(hazard)}
\item Patient completes if \code{dropout_time >= followup_time}
\item Dropped-out patients are excluded from analysis
}

\strong{Proportion parameterization} (\code{type = "proportion"}):
Converts to hazard via \code{hazard = -log(1 - rate) / followup_time}.
This means \code{rate} is the expected proportion of patients who drop out
over the full follow-up period.

\strong{Hazard parameterization} (\code{type = "hazard"}):
Uses \code{rate} directly as the constant hazard rate per time unit,
independent of follow-up time.

\strong{Requirements:}
\itemize{
\item \code{dropout} requires \code{accrual_rate} (dropout without calendar time
is meaningless)
\item \code{dropout} requires \code{followup_time > 0} (instant outcomes can't
have dropout)
}
}
\examples{
\dontrun{
# 20\% expected dropout over follow-up period
build_conditions(
  design = my_design,
  constant = list(dropout = dropout(0.2), followup_time = 12,
                  accrual_rate = 5)
)

# Cross different dropout rates
build_conditions(
  design = my_design,
  crossed = list(dropout = dropout(c(0.1, 0.2, 0.3)))
)

# Hazard rate parameterization
build_conditions(
  design = my_design,
  constant = list(dropout = dropout(0.03, type = "hazard"))
)
}

}
