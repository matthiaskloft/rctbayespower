% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accrual.R
\name{subset_analysis_data}
\alias{subset_analysis_data}
\title{Subset Data to an Analysis Point (Accrual-Aware)}
\usage{
subset_analysis_data(
  full_data,
  current_n,
  followup_time = 0,
  completion_times = NULL
)
}
\arguments{
\item{full_data}{Data frame (possibly with \code{enrollment_time} column).}

\item{current_n}{Integer. Target number of analyzable patients.}

\item{followup_time}{Numeric. Required follow-up per patient.}

\item{completion_times}{Optional pre-sorted vector of completion times
(\code{enrollment_time + followup_time}). Pass this when calling inside a loop
to avoid redundant sorting.}
}
\value{
Data frame with approximately \code{current_n} rows and \code{enrollment_time}
column removed.
}
\description{
Returns the rows of \code{full_data} that are analyzable at the point when
\code{current_n} patients have completed follow-up. When \code{full_data} contains
an \code{enrollment_time} column, subsetting is calendar-time-aware; otherwise
it falls back to simple row-index subsetting (\code{full_data[1:current_n, ]}).
}
\keyword{internal}
