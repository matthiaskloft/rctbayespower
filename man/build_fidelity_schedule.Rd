% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization_internal.R,
%   R/optimization_internal_backup.R
\name{build_fidelity_schedule}
\alias{build_fidelity_schedule}
\title{Build n_sims Schedule for Progressive Fidelity}
\usage{
build_fidelity_schedule(n_sims, max_evals, evals_per_step = 10)

build_fidelity_schedule(n_sims, max_evals, evals_per_step = 10)
}
\arguments{
\item{n_sims}{Scalar or vector of simulation counts. If a vector,
n_sims increases in discrete steps (e.g., \code{c(100, 200, 500, 1000)}).}

\item{max_evals}{Total evaluations (used when n_sims is scalar).}

\item{evals_per_step}{Controls evaluations per fidelity level when n_sims is
a vector. Can be:
\itemize{
\item \strong{Scalar} (default 10): Same number of evaluations at each level.
Total evals = \code{length(n_sims) * evals_per_step}.
\item \strong{Vector}: Custom evaluations per level. Must have same length as n_sims.
Example: \code{c(15, 10, 10, 15)} means 15 evals at level 1, 10 at level 2, etc.
Total evals = \code{sum(evals_per_step)}.
}}
}
\value{
Data frame with columns:
\itemize{
\item \code{eval}: Evaluation number (1 to total_evals)
\item \code{n_sims}: Number of simulations for that evaluation
\item \code{fidelity_level}: Which fidelity level (1 to length(n_sims))
\item \code{is_progressive}: Whether this is progressive fidelity
}

Data frame with columns:
\itemize{
\item \code{eval}: Evaluation number (1 to total_evals)
\item \code{n_sims}: Number of simulations for that evaluation
\item \code{fidelity_level}: Which fidelity level (1 to length(n_sims))
\item \code{is_progressive}: Whether this is progressive fidelity
}
}
\description{
Creates a mapping from evaluation number to n_sims for multi-fidelity
optimization. Supports both constant fidelity (scalar n_sims) and
progressive fidelity (vector n_sims with evals_per_step).

Creates a mapping from evaluation number to n_sims for multi-fidelity
optimization. Supports both constant fidelity (scalar n_sims) and
progressive fidelity (vector n_sims with evals_per_step).
}
\details{
\strong{Constant fidelity} (scalar \code{n_sims}):
All evaluations use the same n_sims. Total evaluations = \code{max_evals}.

\strong{Progressive fidelity with scalar \code{evals_per_step}}:
Equal number of evaluations per level.
Example: \code{n_sims = c(100, 200, 500, 1000)}, \code{evals_per_step = 10} -> 40 total

\strong{Progressive fidelity with vector \code{evals_per_step}}:
Custom number of evaluations per level.
Example: \code{n_sims = c(100, 200, 500, 1000)}, \code{evals_per_step = c(15, 10, 10, 15)}
\itemize{
\item Evals 1-15: n_sims = 100 (15 evals)
\item Evals 16-25: n_sims = 200 (10 evals)
\item Evals 26-35: n_sims = 500 (10 evals)
\item Evals 36-50: n_sims = 1000 (15 evals)
}

\strong{Constant fidelity} (scalar \code{n_sims}):
All evaluations use the same n_sims. Total evaluations = \code{max_evals}.

\strong{Progressive fidelity with scalar \code{evals_per_step}}:
Equal number of evaluations per level.
Example: \code{n_sims = c(100, 200, 500, 1000)}, \code{evals_per_step = 10} -> 40 total

\strong{Progressive fidelity with vector \code{evals_per_step}}:
Custom number of evaluations per level.
Example: \code{n_sims = c(100, 200, 500, 1000)}, \code{evals_per_step = c(15, 10, 10, 15)}
\itemize{
\item Evals 1-15: n_sims = 100 (15 evals)
\item Evals 16-25: n_sims = 200 (10 evals)
\item Evals 26-35: n_sims = 500 (10 evals)
\item Evals 36-50: n_sims = 1000 (15 evals)
}
}
\keyword{internal}
