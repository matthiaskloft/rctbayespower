% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models_ancova.R
\name{simulate_data_ancova_cont_2arms_batch}
\alias{simulate_data_ancova_cont_2arms_batch}
\title{Simulate ANCOVA Data - Batched Format (2-arm)}
\usage{
simulate_data_ancova_cont_2arms_batch(
  n_sims,
  n_total,
  p_alloc = 0.5,
  intercept = 0,
  b_arm_treat = 0,
  b_covariate = 0,
  sigma = 1
)
}
\arguments{
\item{n_sims}{Number of simulations to generate (batch size)}

\item{n_total}{Sample size per simulation}

\item{p_alloc}{Allocation probability for treatment (default 0.5)}

\item{intercept}{Intercept value (default 0)}

\item{b_arm_treat}{Treatment effect coefficient}

\item{b_covariate}{Covariate effect coefficient (default 0)}

\item{sigma}{Residual standard deviation (default 1)}
}
\value{
List with batch-formatted arrays:
\itemize{
\item outcome: matrix (n_sims x n_total)
\item covariate: matrix (n_sims x n_total)
\item group: matrix (n_sims x n_total), binary treatment indicator
\item N: integer, sample size per simulation
\item p_alloc: numeric, allocation probability
}
}
\description{
Generates multiple simulations at once for NPE/BayesFlow efficiency.
Returns matrices instead of data.frames for direct use with neural networks.
}
\details{
This function is optimized for batch generation using vectorized operations.
It generates all simulations simultaneously, making it suitable for
BayesFlow/NPE workflows where batching improves efficiency.

The group matrix contains binary (0/1) values for 2-arm trials,
where 0 = control and 1 = treatment.
}
\examples{
\dontrun{
# Generate 64 simulations with 100 subjects each
batch_data <- simulate_data_ancova_cont_2arms_batch(
  n_sims = 64,
  n_total = 100,
  b_arm_treat = 0.5,
  b_covariate = 0.3
)
dim(batch_data$outcome)  # [64, 100]
}
}
\keyword{internal}
